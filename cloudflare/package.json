{
  "name": "lesmoles-events",
  "version": "1.0.0",
  "private": true,
  "description": "Les Moles Events — app de gestión de bodas y eventos (Cloudflare Worker + D1)",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.20.0"
  }
}
